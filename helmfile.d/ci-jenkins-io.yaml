---
helmDefaults:
  atomic: true
  force: false
  timeout: 600
  wait: true

releases:
  - name: jenkins
    chart: jenkins/jenkins
    version: 3.1.2
    namespace: jenkins
    values:
      - "../config/default/ci-infra-io.yaml"
    set:
      - name: namespace
        value: jenkins
