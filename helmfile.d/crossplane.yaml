helmDefaults:
  atomic: false
  force: false
  timeout: 600
  wait: true
releases:
  - name: crossplane
    chart: crossplane-stable/crossplane
    version: 1.10.1
    namespace: crossplane-system
    values:
      - "../config/default/crossplane.yaml"
    set:
      - name: namespace
        value: crossplane-system
